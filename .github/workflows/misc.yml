name: Other tests

on:
  push:
  pull_request:

jobs:
  git-describe:
    runs-on: ubuntu-latest
    steps:
      - uses: HexDecimal/checkout@b8f2f1e0a50cfa75de15c2d7fef6acbe077c711c
        with:
          fetch-depth: 0
      - name: Run git describe
        run: git describe --always
